<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.effect.DropShadow?>

<BorderPane xmlns="http://javafx.com/javafx/21.0.2" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.baccalaureat.controller.MultiplayerLobbyController"
            styleClass="lobby-container">
    
    <!-- Header -->
    <top>
        <VBox alignment="CENTER" spacing="10">
            <padding>
                <Insets top="30" right="30" bottom="20" left="30"/>
            </padding>
            <HBox alignment="CENTER_LEFT">
                <Button text="← Retour" styleClass="back-button" onAction="#handleBackToMenu"/>
                <Pane HBox.hgrow="ALWAYS"/>
                <Label fx:id="connectionStatusLabel" text="🔄 Connexion..." style="-fx-text-fill: #ffd93d;"/>
            </HBox>
            <Label text="🌐 Multijoueur en ligne" styleClass="lobby-title"/>
            <Label text="Créez ou rejoignez une partie!" styleClass="lobby-subtitle"/>
            <HBox fx:id="sessionIdDisplay" alignment="CENTER" spacing="10" visible="false">
                <Label fx:id="sessionIdLabel" text="Code de session: " 
                       style="-fx-text-fill: #4ecca3; -fx-font-size: 16px; -fx-font-weight: bold;"/>
            </HBox>
        </VBox>
    </top>
    
    <!-- Players List -->
    <center>
        <VBox spacing="20" alignment="TOP_CENTER">
            <padding>
                <Insets top="20" right="50" bottom="20" left="50"/>
            </padding>
            
            <!-- Create or Join Game Section -->
            <VBox spacing="10" alignment="CENTER">
                <HBox spacing="10" alignment="CENTER">
                    <TextField fx:id="playerNameInput" promptText="Votre nom..." 
                               styleClass="player-input" prefWidth="200"/>
                </HBox>
                
                <HBox spacing="10" alignment="CENTER">
                    <Button fx:id="createGameButton" text="🎮 Créer une partie" 
                            styleClass="add-player-button" onAction="#handleCreateGame"/>
                    <Button fx:id="joinGameButton" text="🔗 Rejoindre" 
                            styleClass="add-player-button" onAction="#handleJoinGame"/>
                </HBox>
                
                <TextField fx:id="sessionIdInput" promptText="Code de session (pour rejoindre)..." 
                           styleClass="player-input" prefWidth="200" visible="true"/>
            </VBox>
            
            <!-- Players List -->
            <Label text="Joueurs connectés" styleClass="section-title" style="-fx-font-size: 18px;"/>
            <ListView fx:id="playersListView" prefHeight="150" prefWidth="400" 
                      styleClass="players-list"/>
            
            <!-- Status notice -->
            <Label fx:id="noticeLabel" text="Créez une partie ou rejoignez avec un code" 
                   style="-fx-text-fill: #cccccc; -fx-font-size: 12px;"/>
        </VBox>
    </center>
    
            <!-- Bottom Actions -->
    <bottom>
        <VBox spacing="15" alignment="CENTER">
            <padding>
                <Insets top="20" right="30" bottom="30" left="30"/>
            </padding>
            
            <Button fx:id="startGameButton" text="🎮 Démarrer la partie" 
                    styleClass="menu-button" prefWidth="300" onAction="#handleStartGame" 
                    disable="true" visible="true"/>
        </VBox>
    </bottom>
</BorderPane>
